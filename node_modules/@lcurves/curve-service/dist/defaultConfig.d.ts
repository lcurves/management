export declare let defaultConfig: {
    APP_PORT: number;
    ENV: string;
    REDIS_STORE: {
        host: string;
    };
    SESSION: {
        secret: string;
        cookie: {
            maxAge: number;
        };
        resave: boolean;
        saveUninitialized: boolean;
    };
};
