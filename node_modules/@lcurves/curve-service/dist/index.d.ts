import * as express from 'express';
import * as connectRedis from 'connect-redis';
declare class CurveService {
    config: any;
    environment: string;
    app: express.Express;
    redis: connectRedis.Redis;
    constructor(config: any);
    static allowOrigin(app: express.Express): void;
    static storeSession(app: express.Express, store: connectRedis.Redis, sessionConfig: any): void;
}
export default CurveService;
